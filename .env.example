# ---------------------------------------------------------------------------
# Root environment — consumed by docker-compose only.
# Copy to `.env` for local development. NEVER commit a filled-in copy.
# Application configuration lives in server/.env and client/.env
# ---------------------------------------------------------------------------

POSTGRES_USER=bmd
POSTGRES_PASSWORD=bmd_dev_password
POSTGRES_DB=bmd_studio
POSTGRES_PORT=5432
POSTGRES_TEST_PORT=5433

REDIS_PORT=6379

MINIO_ROOT_USER=bmd_minio
MINIO_ROOT_PASSWORD=bmd_minio_dev_password
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
